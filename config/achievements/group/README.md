# 群专属成就说明

## 📁 目录用途

此目录用于存放特定群组的专属成就配置。每个群组可以有自己独立的成就系统。

## 🏗️ 目录结构

```
config/achievements/group/
  └── {群ID}/                    # 特定群组目录
      ├── group.json             # 群专属成就文件（默认文件名）
      └── custom.json            # 其他自定义成就文件
```

## 📝 使用方法

### 1. 创建群专属成就目录

在 `config/achievements/group/` 目录下创建以群号命名的文件夹，例如：
- `config/achievements/group/123456789/` （群号 123456789）
- `config/achievements/group/987654321/` （群号 987654321）

### 2. 创建成就文件

在群组目录下创建 JSON 文件，文件名可以自定义，但必须以 `.json` 结尾。

### 3. 配置文件示例

```json
{
  "group_first_message": {
    "id": "group_first_message",
    "name": "本群初来乍到",
    "description": "在本群发送第一条消息",
    "rarity": "legendary",
    "category": "basic",
    "condition": {
      "type": "first_message"
    }
  },
  "group_message_100": {
    "id": "group_message_100",
    "name": "本群水王",
    "description": "在本群累计发言100条",
    "rarity": "epic",
    "category": "count",
    "condition": {
      "type": "total_count",
      "value": 100
    }
  }
}
```

## ⚠️ 注意事项

1. **目录名称**：目录名必须是有效的群号（数字）
2. **文件格式**：必须是有效的 JSON 格式
3. **成就 ID**：建议使用 `group_` 前缀，避免与全局成就 ID 冲突
4. **覆盖规则**：群专属成就会覆盖同名 ID 的全局成就

## 🎯 支持的功能

群专属成就支持所有成就类型，包括：
- ✅ 基础类：`first_message`
- ✅ 计数类：`total_count`, `daily_count`, `weekly_count`, `monthly_count`
- ✅ 字数类：`total_words`, `daily_words`, `weekly_words`, `monthly_words`
- ✅ 天数类：`active_days`, `daily_streak`, `max_continuous_days`
- ✅ 时段类：`night_time`, `morning_time`, `time_window`
- ✅ 文本类：`text_contains`, `text_contains_times`

详细说明请参考 [achievements/README.md](../README.md)

## 🔄 加载优先级

成就加载按以下顺序合并（后加载的覆盖先加载的）：

1. 系统默认成就：`config/achievements.json`
2. 用户自定义成就：`config/achievements/*.json`
3. **群专属成就**：`config/achievements/group/{群ID}/*.json`

## 💡 使用建议

1. **文件组织**：建议每个群组的成就存放在独立的文件中
2. **命名规范**：使用有意义的文件名，如 `group.json`, `special.json` 等
3. **定期备份**：群专属配置建议定期备份
4. **测试验证**：创建成就后建议在群内测试解锁条件

