# Web 前端项目结构

## 📁 目录结构

```
resources/web/
├── index.html                    # 主入口HTML文件
├── assets/                       # 静态资源
│   ├── css/                      # 样式文件
│   │   ├── common.css            # 公共样式（布局、导航、基础样式）
│   │   └── components.css         # 组件样式（卡片、按钮、表格等）
│   ├── js/                       # JavaScript文件
│   │   ├── app.js                # 主应用入口
│   │   ├── router.js             # 路由系统
│   │   ├── api.js                # API封装
│   │   ├── utils.js              # 工具函数
│   │   └── components/           # 公共组件
│   │       ├── index.js          # 组件统一导出
│   │       ├── Navbar.js         # 导航栏组件
│   │       ├── Sidebar.js        # 侧边栏组件
│   │       ├── Card.js           # 卡片组件
│   │       ├── Modal.js          # 模态框组件
│   │       ├── Button.js          # 按钮组件
│   │       └── Input.js           # 输入框组件
│   └── lib/                      # 第三方库（可选）
│       └── fabric.min.js         # Fabric.js（用于图片编辑）
└── pages/                        # 页面组件
    ├── Home.js                   # 首页
    ├── Ranking.js                # 排行榜页面
    ├── UserStats.js              # 用户统计页面
    ├── Achievement.js             # 成就页面
    ├── Background.js              # 背景设置页面
    ├── Admin.js                   # 管理页面
    └── templates/                 # HTML模板文件
        └── Home.html              # 首页HTML模板
```

## 🎨 组件系统

### 使用方式

组件已通过 `components/index.js` 统一导出，可在全局使用：

```javascript
// 在页面中使用组件
import { Card, Button } from '/assets/js/components/index.js';

// 或者使用全局实例（已自动注册）
const cardHtml = Card.render({
    title: '标题',
    content: '内容HTML'
});
```

### 可用组件

1. **Navbar** - 导航栏组件
2. **Sidebar** - 侧边栏组件（响应式）
3. **Card** - 卡片组件（普通卡片和统计卡片）
4. **Modal** - 模态框组件（支持confirm、prompt）
5. **Button** - 按钮组件
6. **Input** - 输入框组件

## 📝 开发规范

### HTML模板 vs JS内联

- **优先使用HTML模板**：在 `pages/templates/` 目录创建 `.html` 文件
- **降级方案**：如果模板加载失败，自动使用JS内联HTML

### 样式规范

- 使用 Tailwind CSS 工具类（已引入CDN）
- 自定义样式放在 `assets/css/` 目录
- 遵循响应式设计原则

### 组件开发

1. 组件文件放在 `assets/js/components/`
2. 通过 `index.js` 统一导出
3. 提供 `render()` 方法返回HTML字符串
4. 提供 `init()` 方法初始化事件监听

## 🚀 快速开始

1. **通过QQ命令访问**：
   - 发送 `#水群网页` 获取你的统计网页链接
   - 发送 `#水群设置背景` 获取背景设置页面链接
   - 链接24小时内有效，自动识别你的身份

2. **直接访问**：`http://localhost:39999/`（首次访问需要输入用户ID）

3. **开发调试**：
   - 编辑HTML模板：`pages/templates/Home.html`
   - 编辑页面逻辑：`pages/Home.js`
   - 使用组件：导入 `components/index.js`

## 📚 技术栈

- **Tailwind CSS** - 实用优先的CSS框架
- **原生 JavaScript (ES6+)** - 无框架依赖
- **Fabric.js** - 图片编辑功能
- **Express** - 后端服务器

