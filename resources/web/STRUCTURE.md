# Web 文件夹结构说明

## 📁 完整目录结构

```
resources/web/
├── index.html                    # 主入口HTML文件（SPA路由）
├── README.md                     # 项目说明文档
├── STRUCTURE.md                  # 本文件：文件夹结构说明
│
├── assets/                       # 静态资源目录
│   ├── css/                      # 样式文件
│   │   ├── common.css            # 公共样式
│   │   │                          # - 基础样式、变量定义
│   │   │                          # - 导航栏样式
│   │   │                          # - 主内容区样式
│   │   │                          # - Toast、Modal基础样式
│   │   └── components.css         # 组件样式
│   │                              # - 按钮、卡片样式
│   │                              # - 表格、输入框样式
│   │                              # - 侧边栏样式
│   │                              # - 响应式样式
│   │
│   ├── js/                       # JavaScript文件
│   │   ├── app.js                # 主应用入口
│   │   │                          # - App类：应用初始化
│   │   │                          # - 路由注册
│   │   │                          # - 页面加载逻辑
│   │   │
│   │   ├── router.js             # 路由系统
│   │   │                          # - Router类：路由管理
│   │   │                          # - 路由注册和导航
│   │   │                          # - Hash路由处理
│   │   │
│   │   ├── api.js                # API封装
│   │   │                          # - API类：RESTful API调用
│   │   │                          # - 统计数据API
│   │   │                          # - 排行榜API
│   │   │                          # - 成就API
│   │   │                          # - 背景API
│   │   │
│   │   ├── utils.js              # 工具函数
│   │   │                          # - Toast提示
│   │   │                          # - 格式化函数（数字、日期、相对时间）
│   │   │                          # - URL参数管理
│   │   │                          # - 防抖/节流函数
│   │   │                          # - 存储管理（localStorage）
│   │   │                          # - 模板加载函数
│   │   │
│   │   └── components/           # 公共组件目录
│   │       ├── index.js           # 组件统一导出
│   │       ├── README.md          # 组件使用文档
│   │       ├── Navbar.js          # 导航栏组件
│   │       ├── Sidebar.js         # 侧边栏组件
│   │       ├── Card.js            # 卡片组件
│   │       ├── Modal.js           # 模态框组件
│   │       ├── Button.js          # 按钮组件
│   │       └── Input.js           # 输入框组件
│   │
│   └── lib/                      # 第三方库（可选）
│       └── (可存放第三方JS库)
│
└── pages/                        # 页面组件目录
    ├── Home.js                   # 首页组件
    ├── Ranking.js                # 排行榜页面
    ├── UserStats.js              # 用户统计页面
    ├── Achievement.js             # 成就页面
    ├── Background.js              # 背景设置页面
    ├── Admin.js                   # 管理页面
    └── templates/                 # HTML模板文件目录
        └── Home.html              # 首页HTML模板
                                    # (可继续添加其他页面模板)
```

## 📂 目录说明

### `/assets/css/` - 样式文件
- **common.css**: 全局公共样式，包括基础样式、变量、布局等
- **components.css**: 组件专用样式，包括卡片、按钮、表格等组件样式

### `/assets/js/` - JavaScript文件
- **app.js**: 应用主入口，负责应用初始化和路由注册
- **router.js**: 路由系统，处理页面导航和路由匹配
- **api.js**: API封装层，统一处理所有API请求
- **utils.js**: 工具函数库，提供常用工具方法
- **components/**: 公共组件目录，所有可复用的UI组件

### `/pages/` - 页面组件
- 每个页面是一个独立的JS模块
- 页面类必须实现 `render()` 方法返回HTML
- 可选实现 `mounted()` 方法处理页面初始化逻辑
- **templates/**: HTML模板文件，优先使用模板，失败时降级到JS内联HTML

## 🔧 组件系统

### 组件位置
所有公共组件位于 `/assets/js/components/`

### 组件列表
1. **Navbar** - 顶部导航栏
2. **Sidebar** - 侧边栏（响应式）
3. **Card** - 卡片组件（普通卡片 + 统计卡片）
4. **Modal** - 模态框（支持confirm、prompt）
5. **Button** - 按钮组件
6. **Input** - 输入框组件

### 使用方式
```javascript
// 方式1：导入使用
import { Card, Button } from '/assets/js/components/index.js';

// 方式2：全局实例（已自动注册）
window.Card.render({ ... });
window.Modal.show({ ... });
```

## 📝 开发规范

### 文件命名
- JS文件：PascalCase（如 `Home.js`, `UserStats.js`）
- CSS文件：kebab-case（如 `common.css`, `components.css`）
- HTML模板：PascalCase（如 `Home.html`）

### 代码组织
- 页面逻辑：`pages/*.js`
- HTML模板：`pages/templates/*.html`
- 公共组件：`assets/js/components/*.js`
- 样式文件：`assets/css/*.css`

### 组件开发规范
1. 组件必须提供 `render()` 静态方法或实例方法
2. 组件类使用 ES6 class 语法
3. 通过 `index.js` 统一导出
4. 提供完整的使用文档

## 🚀 快速开始

1. **创建新页面**：
   - 在 `pages/` 创建 `YourPage.js`
   - 实现 `render()` 和 `mounted()` 方法
   - 在 `app.js` 中注册路由

2. **使用组件**：
   ```javascript
   import { Card } from '/assets/js/components/index.js';
   const html = Card.render({ title: '标题', content: '内容' });
   ```

3. **创建HTML模板**：
   - 在 `pages/templates/` 创建 `YourPage.html`
   - 页面会自动优先加载模板文件

## 📚 相关文档

- [组件使用文档](./assets/js/components/README.md)
- [项目说明文档](./README.md)

